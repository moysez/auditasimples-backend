services:
  - type: web
    name: auditasimples-backend
    env: python
    runtime: python-3.11.9
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
